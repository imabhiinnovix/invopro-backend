export async function autoPopulateAttributeOption({
  filePath,
  columnName,
  startRow = 2,
}: {
  filePath: string;
  columnName: string;
  startRow: number;
}) {
  try {
  } catch (e) {
    console.log('Error while auto populating attribute option.', e);
    throw e;
  }
}
