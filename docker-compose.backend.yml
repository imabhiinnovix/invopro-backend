version: '3.8'

services:
  app:
    build: .
    ports:
      - '5000:5000'
    environment:
      - NODE_ENV=development
      - MONGO_URI=mongodb://192.168.1.55:27017/searchify
    volumes:
      - .:/app
      - /app/node_modules
